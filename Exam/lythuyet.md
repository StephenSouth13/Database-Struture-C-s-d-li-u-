
# Câu 1. Tìm khóa của lược đồ quan hệ

Cho lược đồ quan hệ:

```text
R = {A, B, C, D, E}
```

Tập phụ thuộc hàm:

```text
F = {
    AB → C
    CD → E
    B → CE
}
```

Xét tập phụ thuộc hàm tối thiểu:

```text
G = {
    CD → E
    B → CE
    A → C
}
```

Hãy tìm khóa của quan hệ `R`.
